---
phase: 02-calendar-grid
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/pages/ScheduleCalendar.jsx
autonomous: true
requirements:
  - CAL-02
  - CAL-03
  - CAL-04

must_haves:
  truths:
    - "The coordinator can navigate to previous/next weeks using arrow buttons and the grid updates to show that week's workshops"
    - "A Today button returns to the current week when viewing a different week"
    - "The header displays the correct week date range in 'MMM d - MMM d, yyyy' format"
    - "Day/Week/Month view toggle tabs are visible with Week as the active default"
    - "Day and Month tabs show placeholder text; only Week view renders the full calendar grid"
  artifacts:
    - path: "src/pages/ScheduleCalendar.jsx"
      provides: "Week navigation state, prev/next/today controls, view toggle tabs, header date range, CalendarGrid wiring"
      min_lines: 50
  key_links:
    - from: "src/pages/ScheduleCalendar.jsx"
      to: "src/components/calendar/CalendarGrid.jsx"
      via: "passes weekDays derived from currentWeekStart state to CalendarGrid"
      pattern: "weekDays.*CalendarGrid"
    - from: "src/pages/ScheduleCalendar.jsx"
      to: "date-fns"
      via: "startOfWeek, addWeeks, subWeeks, addDays, format, isSameWeek for week navigation"
      pattern: "startOfWeek|addWeeks|subWeeks"
---

<objective>
Add week navigation controls (previous/next arrows and Today button), view toggle tabs (Day/Week/Month), and a header displaying the current week date range to the ScheduleCalendar page.

Purpose: Coordinators need to browse different weeks and see at a glance which week they're viewing. The view toggles establish the UI pattern for future Day/Month views.
Output: Updated ScheduleCalendar.jsx with full navigation bar, view toggles, and dynamic week state.
</objective>

<execution_context>
@/Users/adam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/adam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-calendar-grid/02-RESEARCH.md
@.planning/phases/02-calendar-grid/02-01-SUMMARY.md
@src/pages/ScheduleCalendar.jsx
@src/components/calendar/CalendarGrid.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add week navigation state, controls, and header date range</name>
  <files>
    src/pages/ScheduleCalendar.jsx
  </files>
  <action>
Refactor `src/pages/ScheduleCalendar.jsx` to add week navigation. The static `currentWeekStart` from Plan 01 becomes a `useState`:

1. **State:** `const [currentWeekStart, setCurrentWeekStart] = useState(() => startOfWeek(new Date(), { weekStartsOn: 1 }))`.
2. **Derived values:**
   - `weekDays = Array.from({ length: 7 }, (_, i) => addDays(currentWeekStart, i))`
   - `weekEnd = weekDays[6]`
   - `headerText = format(currentWeekStart, 'MMM d') + ' \u2013 ' + format(weekEnd, 'MMM d, yyyy')` (use en-dash \u2013)
   - `isCurrentWeek = isSameWeek(currentWeekStart, new Date(), { weekStartsOn: 1 })`
3. **Navigation handlers:**
   - `prevWeek = () => setCurrentWeekStart(d => subWeeks(d, 1))`
   - `nextWeek = () => setCurrentWeekStart(d => addWeeks(d, 1))`
   - `goToToday = () => setCurrentWeekStart(startOfWeek(new Date(), { weekStartsOn: 1 }))`
4. **Navigation bar UI** (rendered above the CalendarGrid):
   - Flex row with items-center, gap-3, padding py-3 px-4, bg-white border-b border-border.
   - Left section: ChevronLeft button (onClick prevWeek), headerText in `text-lg font-semibold text-ww-navy`, ChevronRight button (onClick nextWeek).
   - "Today" button: `px-3 py-1.5 text-sm font-medium rounded border border-border hover:bg-surface-2 transition-colors`. When `isCurrentWeek`, add `opacity-50 cursor-default` and set `disabled={isCurrentWeek}`. Text: "Today".
   - ChevronLeft/ChevronRight from lucide-react, size 20, with `text-slate-600 hover:text-ww-navy p-1 rounded hover:bg-surface-2 transition-colors` styling.
5. **Pass `weekDays` to CalendarGrid** (replacing the static computation from Plan 01).
6. **Imports needed:** `useState` from react, `startOfWeek, addWeeks, subWeeks, addDays, format, isSameWeek` from date-fns, `ChevronLeft, ChevronRight` from lucide-react.

IMPORTANT: Always pass `{ weekStartsOn: 1 }` to `startOfWeek` and `isSameWeek` for Monday-anchored weeks.
  </action>
  <verify>
Run `npm run build` — should succeed. Run `npm run dev` and verify:
1. Header shows "Week of Mar 2 - Mar 8, 2026" (or current week range)
2. Clicking left arrow shows previous week's dates and workshops
3. Clicking right arrow shows next week's dates and workshops
4. "Today" button returns to current week when on a different week
5. "Today" button is disabled/dimmed when already on current week
6. Week navigation correctly updates CalendarGrid day columns and workshops
  </verify>
  <done>
Week navigation with prev/next arrows and Today button is functional. Header displays correct "MMM d - MMM d, yyyy" date range. CalendarGrid updates to show the selected week's workshops.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Day/Week/Month view toggle tabs</name>
  <files>
    src/pages/ScheduleCalendar.jsx
  </files>
  <action>
Add view toggle tabs to the ScheduleCalendar navigation bar:

1. **State:** `const [viewMode, setViewMode] = useState('week')`.
2. **View toggle UI:** Render a flex row of 3 tab buttons (Day, Week, Month) in the navigation bar, positioned to the right of the Today button with `ml-auto` for right-alignment (or centered with appropriate spacing).
   - Each tab: `px-4 py-1.5 text-sm font-medium rounded transition-colors`.
   - Active tab: `bg-ww-blue text-white`.
   - Inactive tab: `text-slate-600 hover:bg-surface-2`.
   - Tab labels: `['Day', 'Week', 'Month']`, corresponding values: `['day', 'week', 'month']`.
   - Wrap tabs in a `flex gap-1 bg-surface rounded-lg p-1` container for a segmented control appearance.
3. **Conditional rendering:**
   - When `viewMode === 'week'`: render CalendarGrid (existing).
   - When `viewMode === 'day'`: render `<div className="flex-1 flex items-center justify-center text-slate-400 text-sm">Day view coming soon</div>`.
   - When `viewMode === 'month'`: render `<div className="flex-1 flex items-center justify-center text-slate-400 text-sm">Month view coming soon</div>`.
4. The navigation bar layout should be: `[ChevronLeft] [Week Range] [ChevronRight] [Today] ... [Day|Week|Month]` with the view toggles right-aligned.
  </action>
  <verify>
Run `npm run build` — should succeed. Run `npm run dev` and verify:
1. Three view toggle tabs visible (Day, Week, Month)
2. Week tab is active by default (blue background)
3. Clicking Day tab shows "Day view coming soon" placeholder
4. Clicking Month tab shows "Month view coming soon" placeholder
5. Clicking Week tab returns to the full calendar grid
6. Week navigation arrows still work when on Week view
  </verify>
  <done>
Day/Week/Month view toggle tabs are visible. Week is the default active view showing the full calendar grid. Day and Month show placeholder text. The toggle has a segmented control appearance with active tab highlighted in ww-blue.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds with no errors
2. Navigate to http://localhost:5173/ — calendar with navigation bar visible
3. Header shows "MMM d - MMM d, yyyy" format for current week
4. Left/right arrow buttons navigate between weeks, grid updates correctly
5. "Today" button returns to current week, disabled when already there
6. Day/Week/Month tabs visible, Week active by default
7. Day and Month tabs show placeholder text
8. Switching back to Week tab restores the calendar grid
</verification>

<success_criteria>
- Week navigation (prev/next/today) works correctly, updating the grid and header
- Header displays accurate week date range
- View toggle tabs render with Week as default, Day/Month as placeholders
- All navigation state uses weekStartsOn: 1 for Monday-anchored weeks
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/02-calendar-grid/02-02-SUMMARY.md`
</output>
