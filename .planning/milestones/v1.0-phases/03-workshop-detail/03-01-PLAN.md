---
phase: 03-workshop-detail
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/panel/WorkshopPanel.jsx
  - src/pages/ScheduleCalendar.jsx
  - src/components/calendar/CalendarGrid.jsx
  - src/components/calendar/WorkshopCard.jsx
autonomous: true
requirements:
  - PANEL-01

must_haves:
  truths:
    - "Clicking a workshop card on the calendar opens a 400px panel sliding in from the right"
    - "The panel slide animation takes 200ms with ease timing"
    - "Clicking the overlay backdrop closes the panel"
    - "Pressing Escape closes the panel"
    - "The panel slides out (not snaps) when closed"
  artifacts:
    - path: "src/components/panel/WorkshopPanel.jsx"
      provides: "Slide-in panel shell with overlay backdrop and Escape key listener"
      min_lines: 40
    - path: "src/pages/ScheduleCalendar.jsx"
      provides: "Panel state management (selectedWorkshopId, panelMode, slotContext) and open/close handlers"
      contains: "selectedWorkshopId"
    - path: "src/components/calendar/WorkshopCard.jsx"
      provides: "onClick prop that calls onWorkshopClick with workshop.id"
      contains: "onClick"
    - path: "src/components/calendar/CalendarGrid.jsx"
      provides: "onWorkshopClick prop threaded to WorkshopCard"
      contains: "onWorkshopClick"
  key_links:
    - from: "src/components/calendar/WorkshopCard.jsx"
      to: "src/pages/ScheduleCalendar.jsx"
      via: "onClick -> onWorkshopClick prop chain through CalendarGrid"
      pattern: "onWorkshopClick"
    - from: "src/pages/ScheduleCalendar.jsx"
      to: "src/components/panel/WorkshopPanel.jsx"
      via: "isOpen prop derived from selectedWorkshopId !== null || panelMode === 'create'"
      pattern: "isPanelOpen"
---

<objective>
Build the slide-in panel shell and wire it to workshop card clicks on the calendar grid.

Purpose: Establish the panel infrastructure (open/close state, animation, overlay, keyboard dismiss) that Plan 02 will fill with form content and Plan 03 will extend with create mode.
Output: WorkshopPanel.jsx component, updated ScheduleCalendar with panel state, WorkshopCard/CalendarGrid with click handlers.
</objective>

<execution_context>
@/Users/adam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/adam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-workshop-detail/03-RESEARCH.md
@.planning/phases/02-calendar-grid/02-01-SUMMARY.md
@.planning/phases/02-calendar-grid/02-02-SUMMARY.md
@src/pages/ScheduleCalendar.jsx
@src/components/calendar/CalendarGrid.jsx
@src/components/calendar/WorkshopCard.jsx
@src/context/AppContext.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create WorkshopPanel shell and add panel state to ScheduleCalendar</name>
  <files>
    src/components/panel/WorkshopPanel.jsx
    src/pages/ScheduleCalendar.jsx
  </files>
  <action>
Create `src/components/panel/WorkshopPanel.jsx`:
- Props: `isOpen`, `onClose`, `children` (panel content will be added by Plan 02)
- Always rendered in the DOM (never conditionally mounted) so exit animation plays
- Overlay backdrop: `fixed inset-0 bg-black/30 z-20`, transitions `opacity-100`/`opacity-0` with `pointer-events-auto`/`pointer-events-none` toggled by `isOpen`
- Panel div: `fixed right-0 top-0 h-screen w-[400px] bg-white z-30 shadow-2xl`, transitions `translate-x-0`/`translate-x-full` toggled by `isOpen`, `transition-transform duration-200 ease-in-out`
- Overlay `onClick={onClose}` to close on backdrop click
- Panel header: flex row with title "Workshop Details" (hardcoded for now, Plan 02 will make dynamic) and an X (lucide-react `X` icon) close button on the right
- Escape key listener: `useEffect` that adds `keydown` listener when `isOpen` is true, calls `onClose()` on Escape, cleans up via `AbortController.abort()`. Dependency array: `[isOpen, onClose]`
- Panel body: `<div className="flex-1 overflow-y-auto p-5">{children}</div>` renders children passed in
- Panel layout: `flex flex-col h-full` so header is fixed at top and body scrolls

Update `src/pages/ScheduleCalendar.jsx`:
- Add three state variables:
  - `const [selectedWorkshopId, setSelectedWorkshopId] = useState(null)`
  - `const [panelMode, setPanelMode] = useState('view')` — values: 'view' | 'create'
  - `const [slotContext, setSlotContext] = useState(null)` — shape: `{ date, hour, minute }` | null
- Compute derived values:
  - `const isPanelOpen = selectedWorkshopId !== null || panelMode === 'create'`
  - `const selectedWorkshop = workshops.find(w => w.id === selectedWorkshopId) ?? null`
- Add `openWorkshop` callback: `useCallback((id) => { setSelectedWorkshopId(id); setPanelMode('view'); setSlotContext(null); }, [])`
- Add `closePanel` callback: `useCallback(() => { setSelectedWorkshopId(null); setPanelMode('view'); setSlotContext(null); }, [])`
- Import WorkshopPanel and render it AFTER the calendar body div (still inside the outer flex container, but as a sibling): `<WorkshopPanel isOpen={isPanelOpen} onClose={closePanel}>{/* Plan 02 fills content */}</WorkshopPanel>`
- Add `useCallback` to imports from react
  </action>
  <verify>
Run `npm run build` — no errors. Open browser, verify panel shell is present in DOM (hidden off-screen via translate-x-full). Verify no visual change to calendar layout.
  </verify>
  <done>
WorkshopPanel.jsx exists with slide animation and overlay; ScheduleCalendar has panel state (selectedWorkshopId, panelMode, slotContext) and WorkshopPanel rendered always-mounted. Build passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire workshop card click through CalendarGrid to open panel</name>
  <files>
    src/components/calendar/WorkshopCard.jsx
    src/components/calendar/CalendarGrid.jsx
    src/pages/ScheduleCalendar.jsx
  </files>
  <action>
Update `src/components/calendar/WorkshopCard.jsx`:
- Add `onClick` prop to the component: `{ workshop, coachMap, onClick }`
- On the outer `<div>`, add `onClick={(e) => { e.stopPropagation(); onClick?.(workshop.id); }}` — `e.stopPropagation()` prevents the day column's future slot click (Plan 03) from firing
- Keep existing className and all visual rendering unchanged

Update `src/components/calendar/CalendarGrid.jsx`:
- Add `onWorkshopClick` prop: `{ weekDays, workshops, coaches, onWorkshopClick }`
- Pass `onClick={onWorkshopClick}` to each `<WorkshopCard>` instance in the dayWorkshops map

Update `src/pages/ScheduleCalendar.jsx`:
- Pass `onWorkshopClick={openWorkshop}` prop to `<CalendarGrid>`
  </action>
  <verify>
Run `npm run build` — no errors. Click any workshop card in the browser — the 400px panel should slide in from the right with overlay. Click overlay or press Escape — panel slides out. Click a different card — panel opens with slide animation.
  </verify>
  <done>
Clicking any workshop card on the calendar opens the slide-in panel. Panel closes on overlay click or Escape. Card clicks do not bubble to column background. Build passes.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` passes with no errors
2. Workshop card click opens panel with 200ms slide-in animation
3. Overlay click closes panel with 200ms slide-out animation
4. Escape key closes panel
5. Panel is 400px wide, positioned at right edge of screen
6. Panel header shows "Workshop Details" with X close button
7. No visual regression on calendar layout when panel is closed
</verification>

<success_criteria>
- WorkshopPanel.jsx exists with slide animation, overlay, Escape listener
- ScheduleCalendar owns panel state (selectedWorkshopId, panelMode, slotContext)
- WorkshopCard fires onClick with workshop.id, stopPropagation prevents bubbling
- CalendarGrid threads onWorkshopClick to WorkshopCard
- Panel opens and closes with smooth 200ms CSS transition (no snap)
- `npm run build` succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/03-workshop-detail/03-01-SUMMARY.md`
</output>
