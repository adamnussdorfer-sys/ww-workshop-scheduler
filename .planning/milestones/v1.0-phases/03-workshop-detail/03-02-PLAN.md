---
phase: 03-workshop-detail
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/components/panel/WorkshopForm.jsx
  - src/components/panel/AttendanceSparkline.jsx
  - src/components/panel/WorkshopPanel.jsx
autonomous: true
requirements:
  - PANEL-02
  - PANEL-03
  - PANEL-04
  - PANEL-05
  - PANEL-06

must_haves:
  truths:
    - "Panel shows all editable fields: title, date/time with timezone, coach dropdown, co-coach dropdown, type, description, recurrence, and market checkboxes"
    - "Panel shows a status badge (Draft yellow, Published green, Cancelled slate)"
    - "Panel shows a 5-week attendance sparkline for published workshops"
    - "Panel shows a conflict warnings placeholder (empty for now, renders when conflicts array is non-empty)"
    - "Save Draft button saves changes to app state and closes panel"
    - "Publish button sets status to Published and saves to app state"
    - "Remove from Schedule button sets status to Cancelled and closes panel"
  artifacts:
    - path: "src/components/panel/WorkshopForm.jsx"
      provides: "Full editable form with controlled inputs, status badge, action buttons wired to setWorkshops"
      min_lines: 120
    - path: "src/components/panel/AttendanceSparkline.jsx"
      provides: "SVG polyline sparkline for 5-point attendance data"
      min_lines: 15
    - path: "src/components/panel/WorkshopPanel.jsx"
      provides: "Updated shell rendering WorkshopForm with correct props"
      contains: "WorkshopForm"
  key_links:
    - from: "src/components/panel/WorkshopForm.jsx"
      to: "src/context/AppContext.jsx"
      via: "useApp() hook to get setWorkshops for Save/Publish/Remove"
      pattern: "setWorkshops"
    - from: "src/components/panel/WorkshopPanel.jsx"
      to: "src/components/panel/WorkshopForm.jsx"
      via: "renders WorkshopForm with workshop, coaches, mode, onClose props"
      pattern: "WorkshopForm"
    - from: "src/components/panel/WorkshopForm.jsx"
      to: "src/components/panel/AttendanceSparkline.jsx"
      via: "renders sparkline when workshop is Published and has attendance data"
      pattern: "AttendanceSparkline"
---

<objective>
Build the panel form with all editable fields, status badge, attendance sparkline, conflict stub, and action buttons wired to global state.

Purpose: Make the panel functional for viewing and editing existing workshops. This is the core editing surface of the app.
Output: WorkshopForm.jsx, AttendanceSparkline.jsx, updated WorkshopPanel.jsx.
</objective>

<execution_context>
@/Users/adam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/adam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-workshop-detail/03-RESEARCH.md
@.planning/phases/03-workshop-detail/03-01-SUMMARY.md
@src/pages/ScheduleCalendar.jsx
@src/components/panel/WorkshopPanel.jsx
@src/context/AppContext.jsx
@src/data/workshops.js
@src/data/coaches.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create WorkshopForm with all fields, status badge, sparkline, and conflict stub</name>
  <files>
    src/components/panel/WorkshopForm.jsx
    src/components/panel/AttendanceSparkline.jsx
  </files>
  <action>
Create `src/components/panel/AttendanceSparkline.jsx`:
- Props: `data` (array of numbers), `width` (default 80), `height` (default 24)
- Return null if `!data || data.length < 2`
- Compute min, max, range (fallback range=1 to avoid divide-by-zero)
- Map data to SVG `<polyline>` points: `x = (i / (data.length - 1)) * width`, `y = height - ((v - min) / range) * height`
- Render `<svg>` with `viewBox="0 0 {width} {height}"`, `className="overflow-visible"`:
  - `<polyline>` with `fill="none"`, `stroke="currentColor"`, `strokeWidth="1.5"`, `strokeLinejoin="round"`, `strokeLinecap="round"`, `className="text-ww-blue"`
  - `<circle>` at the last data point, `r="2"`, `fill="currentColor"`, `className="text-ww-blue"`

Create `src/components/panel/WorkshopForm.jsx`:
- Props: `workshop` (object or null), `coaches` (array), `mode` ('view' | 'create'), `slotContext` ({ date, hour, minute } | null), `onClose` (function)
- Import `useApp` from context to get `setWorkshops`
- Import `format`, `parseISO` from date-fns
- Import `AttendanceSparkline`

**Local draft state:**
- `const [draft, setDraft] = useState(() => initDraft(workshop, mode, slotContext))`
- Helper function `initDraft`: if mode is 'view' and workshop exists, spread `{ ...workshop }`; if mode is 'create', return `{ title: '', type: 'Weekly Connection', status: 'Draft', coachId: '', coCoachId: null, description: '', recurrence: 'weekly', markets: ['US'], startTime: slotContext ? buildISO(slotContext) : '', endTime: slotContext ? buildEndISO(slotContext) : '' }`
- `buildISO(slotContext)` helper: construct an ISO string from `slotContext.date`, `slotContext.hour`, `slotContext.minute` using `setHours`/`setMinutes` from date-fns
- `buildEndISO(slotContext)` helper: same as buildISO but adds 1 hour to the hour (default 60-min workshop)
- `useEffect` with `[workshop?.id, mode]` dependency array to re-initialize draft when a different workshop is selected (prevents stale form state per research pitfall 2). Use `key={selectedWorkshopId}` approach in WorkshopPanel instead if simpler — decide at implementation time.

**Field updater:**
- `const updateField = (field, value) => setDraft(prev => ({ ...prev, [field]: value }))`
- For markets checkboxes: `toggleMarket(market)` toggles presence in `draft.markets` array

**Workshop type options:** `['Weekly Connection', 'All In', 'Special Event', 'Coaching Corner', 'Movement/Fitness']`
**Market options:** `['US', 'CA', 'UK', 'ANZ']`
**Recurrence options:** `['weekly', 'biweekly', 'monthly', 'one-time']`

**Status badge:** Render at top of form. Color mapping: Draft = `bg-yellow-100 text-yellow-800`, Published = `bg-green-100 text-green-800`, Cancelled = `bg-slate-100 text-slate-600`. Display `draft.status` text inside a rounded pill `<span>`.

**Conflict warnings stub (PANEL-05):**
- Accept a `conflicts` prop (default `[]`)
- If `conflicts.length > 0`, render a red alert box: `bg-red-50 border border-red-200 rounded-lg p-3 mb-4` with `AlertTriangle` icon (lucide-react) and text listing conflicts
- For now this will always be empty — Phase 4 will supply real conflict data

**Attendance sparkline (PANEL-04):**
- Only render when `mode === 'view'` AND `draft.status === 'Published'` AND `draft.attendance` exists
- Show label "Attendance (last 5 weeks)" with `<AttendanceSparkline data={draft.attendance} />` beside it
- Also show the latest attendance number as text: `draft.attendance[draft.attendance.length - 1]`

**Form fields layout (PANEL-02):**
Use `<label>` + `<input>/<select>/<textarea>` pairs. Tailwind form styling: `w-full px-3 py-2 border border-border rounded-lg text-sm focus:outline-none focus:ring-2 focus:ring-ww-blue/30 focus:border-ww-blue`.

Fields in order:
1. **Title** — text input, `value={draft.title}`, `onChange={e => updateField('title', e.target.value)}`
2. **Date & Time** — two `<input type="datetime-local">` side by side for start and end. Convert ISO string to datetime-local format (`yyyy-MM-dd'T'HH:mm`) for value; convert back on change
3. **Timezone** — read-only text display showing "Eastern Time (ET)" (hardcoded for prototype)
4. **Coach** — `<select>` with all coaches as options. Value is `draft.coachId`. For now render all coaches without availability filtering (Plan 03 adds availability). Show coach name in option text.
5. **Co-Coach** — `<select>` with a "None" first option (value `""`), then all coaches. Value is `draft.coCoachId || ''`. Set to null when "" selected.
6. **Type** — `<select>` with workshop type options
7. **Description** — `<textarea>` with 3 rows, `value={draft.description}`
8. **Recurrence** — `<select>` with recurrence options. Capitalize display labels.
9. **Markets** — checkbox group. Each market renders a checkbox + label. Checked if `draft.markets.includes(market)`. onChange calls `toggleMarket`.

Use consistent spacing: `space-y-4` wrapper, each field group has a `<label className="block text-sm font-medium text-slate-700 mb-1">` and the input below it.
  </action>
  <verify>
`npm run build` — no errors. Both files exist. AttendanceSparkline renders an SVG polyline when given data. WorkshopForm renders all fields.
  </verify>
  <done>
WorkshopForm.jsx exists with all 9 editable field groups, status badge, attendance sparkline, and conflict stub. AttendanceSparkline.jsx renders SVG polyline for 5-point data. Build passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire action buttons and integrate WorkshopForm into WorkshopPanel</name>
  <files>
    src/components/panel/WorkshopForm.jsx
    src/components/panel/WorkshopPanel.jsx
    src/pages/ScheduleCalendar.jsx
  </files>
  <action>
Add action buttons to `src/components/panel/WorkshopForm.jsx` (at the bottom of the form, below all fields):

**Button section:** `<div className="flex gap-3 pt-4 border-t border-border mt-6">`

1. **Save Draft** button:
   - `className="flex-1 px-4 py-2 text-sm font-medium bg-surface-2 text-ww-navy rounded-lg hover:bg-slate-200 transition-colors"`
   - onClick `handleSaveDraft`:
     - If mode is 'create': create new workshop `{ ...draft, id: 'ws-' + Date.now() }` and call `setWorkshops(prev => [...prev, newWorkshop])`
     - If mode is 'view': update existing: `setWorkshops(prev => prev.map(w => w.id === draft.id ? { ...draft } : w))`
     - Call `onClose()`

2. **Remove from Schedule** button:
   - Only render when `mode === 'view'` (can't remove something not yet created)
   - `className="px-4 py-2 text-sm font-medium text-red-600 border border-red-200 rounded-lg hover:bg-red-50 transition-colors"`
   - onClick `handleRemove`: `setWorkshops(prev => prev.map(w => w.id === draft.id ? { ...w, status: 'Cancelled' } : w))` then `onClose()`
   - Sets status to 'Cancelled' (not delete from array) per research recommendation — matches existing CalendarGrid filter pattern

3. **Publish** button:
   - `className="flex-1 px-4 py-2 text-sm font-medium bg-ww-blue text-white rounded-lg hover:bg-ww-navy transition-colors"`
   - onClick `handlePublish`:
     - If mode is 'create': `setWorkshops(prev => [...prev, { ...draft, id: 'ws-' + Date.now(), status: 'Published' }])`
     - If mode is 'view': `setWorkshops(prev => prev.map(w => w.id === draft.id ? { ...draft, status: 'Published' } : w))`
     - Call `onClose()`

Update `src/components/panel/WorkshopPanel.jsx`:
- Change props to: `{ isOpen, onClose, workshop, coaches, mode, slotContext }`
- Remove `children` prop pattern — WorkshopPanel now renders WorkshopForm directly
- Import WorkshopForm
- In the panel body div, render: `{isOpen && <WorkshopForm workshop={workshop} coaches={coaches} mode={mode} slotContext={slotContext} onClose={onClose} key={workshop?.id ?? 'create'} />}`
- The `key` prop forces a full remount when switching workshops, which resets local draft state cleanly (avoids stale form state pitfall)
- Only render WorkshopForm when `isOpen` is true to avoid rendering form with null workshop when panel is closed (the panel div itself stays always-mounted for animation; only the form content conditionally renders)
- Update header title: Show "New Workshop" when `mode === 'create'`, otherwise show `workshop?.title ?? 'Workshop Details'`

Update `src/pages/ScheduleCalendar.jsx`:
- Remove the `{children}` approach if it was used in Plan 01
- Pass full props to WorkshopPanel: `<WorkshopPanel isOpen={isPanelOpen} onClose={closePanel} workshop={selectedWorkshop} coaches={coaches} mode={panelMode} slotContext={slotContext} />`
  </action>
  <verify>
`npm run build` — no errors. Click a workshop card — panel opens showing all fields populated with workshop data. Edit a field, click Save Draft — changes persist (card on calendar updates). Click Publish — status changes. Click Remove from Schedule — workshop disappears from calendar (Cancelled filter). Attendance sparkline shows for published workshops.
  </verify>
  <done>
Save Draft, Remove from Schedule, and Publish buttons work and update global workshops state. WorkshopPanel renders WorkshopForm with correct props. Panel title is dynamic. Form re-initializes when switching between workshops. Build passes.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` passes with no errors
2. Panel form shows all 9 field groups when a workshop card is clicked
3. Status badge displays correct color for Draft/Published workshops
4. Attendance sparkline renders for published workshops with attendance data
5. Save Draft persists edited field values to app state
6. Publish changes workshop status to Published
7. Remove from Schedule sets status to Cancelled (workshop disappears from calendar)
8. Switching between workshop cards resets form to the new workshop's data
9. Conflict warnings section renders nothing (empty conflicts array) — ready for Phase 4
</verification>

<success_criteria>
- WorkshopForm.jsx has all editable fields: title, date/time, timezone, coach, co-coach, type, description, recurrence, markets
- Status badge rendered with correct Draft/Published/Cancelled colors
- AttendanceSparkline.jsx renders SVG polyline for published workshops
- Conflict stub renders red alert box only when conflicts array is non-empty
- Three action buttons (Save Draft, Remove, Publish) correctly mutate workshops state via setWorkshops
- Form reinitializes via key prop when selected workshop changes
- `npm run build` succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/03-workshop-detail/03-02-SUMMARY.md`
</output>
