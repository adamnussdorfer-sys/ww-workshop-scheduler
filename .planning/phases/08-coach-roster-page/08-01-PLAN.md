---
phase: 08-coach-roster-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/panel/CoachDetailPanel.jsx
  - src/pages/CoachRoster.jsx
autonomous: true
requirements:
  - ROST-01
  - ROST-02
  - ROST-03
  - ROST-04
  - ROST-05
  - ROST-06

must_haves:
  truths:
    - "All 18 coaches are visible in a table with name, workshop count, and status badge columns"
    - "Clicking a column header sorts the table by that column; clicking again toggles asc/desc"
    - "Typing in the search field filters coaches by name with each keystroke"
    - "Clicking a coach row opens a slide-in detail panel showing coach info and availability"
    - "Escape key closes the coach detail panel"
  artifacts:
    - path: "src/components/panel/CoachDetailPanel.jsx"
      provides: "Coach detail display with all fields and availability schedule"
      min_lines: 30
    - path: "src/pages/CoachRoster.jsx"
      provides: "Sortable, searchable coach table with inline slide-in panel"
      min_lines: 80
  key_links:
    - from: "src/pages/CoachRoster.jsx"
      to: "src/components/panel/CoachDetailPanel.jsx"
      via: "import and render inside panel body"
      pattern: "import CoachDetailPanel"
    - from: "src/pages/CoachRoster.jsx"
      to: "useApp()"
      via: "reads coaches array from AppContext"
      pattern: "const.*coaches.*useApp"
    - from: "src/pages/CoachRoster.jsx"
      to: "panel open state"
      via: "selectedCoachId drives panel visibility"
      pattern: "setSelectedCoachId"
---

<objective>
Build the Coach Roster page: a sortable, searchable table of all 18 coaches with a slide-in detail panel on row click. Replaces the existing CoachRoster.jsx stub.

Purpose: Gives coordinators a dedicated view to review coach assignments, availability status, and workshop load — complementing the calendar's workshop-centric view with a people-centric view.

Output: Two files — CoachDetailPanel.jsx (pure display) and CoachRoster.jsx (full page with table, sort, search, panel).
</objective>

<execution_context>
@/Users/adam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/adam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-coach-roster-page/08-RESEARCH.md
@src/components/panel/WorkshopPanel.jsx
@src/context/AppContext.jsx
@src/data/coaches.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CoachDetailPanel display component</name>
  <files>src/components/panel/CoachDetailPanel.jsx</files>
  <action>
Create `src/components/panel/CoachDetailPanel.jsx` as a pure display component (no state, no effects). It receives a single `coach` prop and renders:

1. **Status badge** at the top — use a `COACH_STATUS_BADGE` lookup object with lowercase keys:
   - `active`: `bg-green-100 text-green-800`
   - `inactive`: `bg-slate-100 text-slate-600`
   Display text with first letter capitalized: `coach.status.charAt(0).toUpperCase() + coach.status.slice(1)`.
   Badge classes: `inline-block px-2.5 py-0.5 rounded-full text-xs font-medium`.

2. **Core info section** — a `space-y-2` container with labeled rows for:
   - Type (`coach.type`)
   - Employment (`coach.employment`)
   - Timezone (`coach.timezone`)
   - Email (`coach.email`)
   - Workshops This Week (`coach.workshopsThisWeek`)
   - Avg Attendance (`coach.avgAttendance`)
   - Attendance Trend (`coach.attendanceTrend` — capitalize first letter)
   Each row: a `DetailRow` helper (defined in-file, not exported) rendering label in `text-xs font-medium text-slate-500` and value in `text-sm text-slate-900`. Use flex justify-between layout.

3. **Availability schedule** — section header "Availability" in `text-xs font-semibold text-slate-500 uppercase tracking-wider mb-2`. Map `coach.availability` array to rows showing day abbreviation and time range. Use a `AVAILABILITY_DAY_LABELS` lookup (`monday` -> `Mon`, etc.). Each row: `flex justify-between text-sm text-slate-600 py-1 border-b border-border last:border-0`. Format time range as `{slot.start} – {slot.end}` (use en-dash character).

Guard: `if (!coach) return null;`

Wrapper: `<div className="space-y-4">`.

No imports needed beyond React (no icons, no hooks).
  </action>
  <verify>File exists at `src/components/panel/CoachDetailPanel.jsx`. Exports a default function component. Contains COACH_STATUS_BADGE and AVAILABILITY_DAY_LABELS lookups. Has `if (!coach) return null` guard.</verify>
  <done>CoachDetailPanel renders coach status badge, all core info fields, and availability schedule as a pure display component.</done>
</task>

<task type="auto">
  <name>Task 2: Build CoachRoster page with sortable table, search, and slide-in panel</name>
  <files>src/pages/CoachRoster.jsx</files>
  <action>
Replace the stub in `src/pages/CoachRoster.jsx` with the full coach roster page. Import `{ useState, useMemo, useCallback, useEffect }` from React, `{ ChevronUp, ChevronDown, ChevronsUpDown, Search, X }` from lucide-react, `{ useApp }` from `../context/AppContext`, and `CoachDetailPanel` from `../components/panel/CoachDetailPanel`.

**State:**
- `sort` — `useState({ key: 'name', direction: 'asc' })` for sort column and direction
- `search` — `useState('')` for search input value
- `selectedCoachId` — `useState(null)` for panel open state

**Derived values:**
- `selectedCoach` — `coaches.find((c) => c.id === selectedCoachId) ?? null`
- `isPanelOpen` — `selectedCoachId !== null`

**Callbacks:**
- `openCoach(id)` — `useCallback` wrapping `setSelectedCoachId(id)`
- `closePanel()` — `useCallback` wrapping `setSelectedCoachId(null)`
- `handleSort(key)` — regular function: if `sort.key === key`, toggle direction; else set new key with `'asc'`

**Computed rows (useMemo):**
`visibleCoaches` — depends on `[coaches, search, sort]`:
1. If `search.trim()` is non-empty, filter coaches where `c.name.toLowerCase().includes(q)` with `q = search.toLowerCase()`
2. Spread before sort: `[...rows].sort(...)`. Compare `a[sort.key]` vs `b[sort.key]` respecting `sort.direction`.
3. Return sorted rows.

**Escape key handler:**
Add a `useEffect` that listens for `keydown` event on `document`. When `e.key === 'Escape'` and `isPanelOpen`, call `closePanel()`. Guard against input/textarea/select/contentEditable focus (same pattern as `useKeyboardShortcuts`). Use `AbortController` for cleanup.

**Layout structure:**
```
<div className="flex flex-col h-full">
  {/* Page header */}
  <div className="flex items-center justify-between px-6 py-4 border-b border-border flex-shrink-0">
    <div>
      <h1 className="text-xl font-semibold text-ww-navy">Coach Roster</h1>
      <p className="text-sm text-slate-500 mt-0.5">{coaches.length} coaches</p>
    </div>
    {/* Search input */}
    <div className="relative">
      <Search size={16} className="absolute left-3 top-1/2 -translate-y-1/2 text-slate-400" />
      <input ... controlled by search state, placeholder="Search coaches..."
        className="pl-9 pr-8 py-2 text-sm border border-border rounded-lg bg-white focus:outline-none focus:ring-2 focus:ring-ww-blue/30 focus:border-ww-blue w-64" />
      {search && <button to clear search — X icon at right side of input />}
    </div>
  </div>

  {/* Table container — scrollable */}
  <div className="flex-1 overflow-auto px-6 py-4">
    <table className="w-full">
      <thead>
        <tr className="border-b border-border">
          {/* 3 SortHeader columns: Name (key 'name'), Workshops (key 'workshopsThisWeek'), Status (key 'status') */}
        </tr>
      </thead>
      <tbody>
        {visibleCoaches.map(coach => <tr> ... </tbody>
    </table>
    {/* Empty search state */}
    {visibleCoaches.length === 0 && search.trim() && (
      <div className="text-center py-12 text-slate-500">
        <p>No coaches matching "{search}"</p>
        <button onClick={() => setSearch('')}>Clear search</button>
      </div>
    )}
  </div>

  {/* Inline panel — copied from WorkshopPanel.jsx pattern */}
  {/* Overlay */}
  {/* Slide panel with CoachDetailPanel inside */}
</div>
```

**SortHeader helper** (defined in-file, not exported):
A `<th>` wrapping a `<button>` with `onClick={() => onSort(sortKey)}`. Shows `ChevronUp` when active+asc, `ChevronDown` when active+desc, `ChevronsUpDown` (faded, `text-slate-300`) when inactive. Button classes: `flex items-center gap-1 text-xs font-semibold text-slate-500 uppercase tracking-wider hover:text-ww-navy transition-colors`. Add `px-4 py-3` padding to the `<th>`. Use `text-left` for Name, `text-center` for Workshops.

**Table row:**
Each `<tr>` has `key={coach.id}`, `onClick={() => openCoach(coach.id)}`, `className="cursor-pointer hover:bg-surface-2 transition-colors border-b border-border last:border-0"`. Columns:
- Name: `px-4 py-3 text-sm font-medium text-ww-navy` — show `coach.name`
- Workshops: `px-4 py-3 text-sm text-slate-600 text-center` — show `coach.workshopsThisWeek`
- Status: `px-4 py-3` — render status badge (same COACH_STATUS_BADGE pattern from CoachDetailPanel, define it again in this file or import — define again is simpler, keeps file self-contained)

**Inline panel shell** (copied from WorkshopPanel.jsx):
- Overlay: `fixed inset-0 bg-black/30 z-20 transition-opacity duration-200` toggled by isPanelOpen (opacity + pointer-events)
- Panel: `fixed right-0 top-0 h-screen w-[400px] bg-white z-30 shadow-2xl flex flex-col transition-transform duration-200 ease-in-out` toggled by `translate-x-0` / `translate-x-full`
- Header: coach name (or "Coach Details" fallback), X close button
- Body: `{isPanelOpen && <CoachDetailPanel coach={selectedCoach} />}` — gate body render so panel doesn't flash stale data on close

Do NOT modify WorkshopPanel.jsx. The coach roster gets its own inline panel markup per research decision (Option A).
  </action>
  <verify>Run `npx vite build` to verify no import errors or missing exports. Manually verify: (1) page renders table with all coach names, (2) clicking column headers toggles sort indicators, (3) typing in search filters rows, (4) clicking a row opens the slide-in panel with coach details, (5) pressing Escape closes the panel.</verify>
  <done>CoachRoster page shows all 18 coaches in a sortable, searchable table. Clicking any row opens a slide-in panel with full coach details. Escape closes the panel. No modifications to existing components.</done>
</task>

</tasks>

<verification>
1. `npx vite build` succeeds with no errors
2. Navigate to /roster — 18 coach rows visible in table
3. Click "Name" header — rows sort alphabetically; click again — reverses to Z-A
4. Click "Workshops" header — sorts by workshop count
5. Click "Status" header — sorts by active/inactive status
6. Type "Sarah" in search — only Sarah Mitchell row remains; clear search restores all
7. Click any coach row — panel slides in from right with overlay
8. Panel shows coach name, status badge, type, employment, timezone, email, workshops count, avg attendance, attendance trend, and availability schedule
9. Press Escape — panel closes
10. Two inactive coaches (Curtis Nganga, Camille Tran) show slate "Inactive" badges; all others show green "Active" badges
</verification>

<success_criteria>
- All 18 coaches displayed in a table at /roster route
- Table sortable by name, workshops, and status columns with asc/desc toggle
- Search input filters coaches by name in real-time
- Row click opens slide-in detail panel with comprehensive coach information
- Panel closes via X button or Escape key
- No modifications to existing components (WorkshopPanel, ScheduleCalendar, etc.)
- Build succeeds with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/08-coach-roster-page/08-01-SUMMARY.md`
</output>
